#application.yml是用户级别的配置
#bootstrap.yml 是系统级别的，优先级别的更高
#config  client 可以通过  config server 拉取到github 上面的 配置
spring:
  cloud:
    config:
      name: eureka-github-config
      profile: dev
      label: master
      uri: http://config-3344.com:3344

management:
  security:
    enabled: false