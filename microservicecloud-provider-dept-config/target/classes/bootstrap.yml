spring:
  cloud:
    config:
      uri: http://config-3344.com:3344
      name: dept-provider-github-config  #需要 从github上读取的配置文件名称，注意不需要 .yml后缀
      profile: dev
      label: master

      
      
management:
 security:
    enabled: false